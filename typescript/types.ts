const n : Number = 1;

let count : Number = 5;

let names : string[] = ['John', 'Anna', 'Rosa'];

let namesAny : any[] = ['Richard', 3, 'Robert'];

enum Starks {Jon, Bran, Eddward, Catlyn};

let cat : Starks = Starks.Bran;

function getName() : string {
    return 'abc';
}
