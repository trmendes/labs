// we just created the network namespace
// communicator::network::conenect
fn connect() {
    println!("network connect");
}
// communicator::network:server::connect
// server is a sub-module of network
mod server;
